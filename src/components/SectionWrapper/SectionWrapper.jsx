import React from "react";

// Styles
import Style from "./SectionWrapper.module.css";

const SectionWrapper = () => {
  return <div className={Style.sectionWrapper}>SectionWrapper</div>;
};

export default SectionWrapper;
